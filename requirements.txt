# --- Data collection ---
# Primary: direct JSON API call with Chrome TLS impersonation
curl-cffi==0.7.3

# Fallback: headless browser (intercepts the same API call)
playwright==1.60.0
playwright-stealth==1.0.6

# Setup tools (required by playwright-stealth 1.0.6)
setuptools<70.0.0

# --- Visualization ---
matplotlib==3.9.0

# --- Additional Scraper Fallbacks ---
cloudscraper==1.2.71
requests==2.32.3
